#!/bin/bash

docker exec -it {{ mongodb_router_container_name }} bash -c "echo 'sh.addShard(\"{{ shard3_set_name }}/{{ host3 }}:{{ mongodb_shard3_port }}\")' | mongo admin -u {{ mongo_admin }} -p {{ mongo_password }}"